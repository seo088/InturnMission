services:
  db:
    image: postgres:15
    container_name: petgraph_db
    restart: always
    ports:
      - "5435:5432"
    environment:
      POSTGRES_USER: myuser
      POSTGRES_PASSWORD: mypassword
      POSTGRES_DB: petdb
    volumes:
      - ./postgres_data:/var/lib/postgresql/data
