import { useState } from 'react'
import { Link } from 'react-router-dom'
import { getPetProfiles } from '../demo/dashboardState'
import '../styles/care-schedule.css'

const base = [{id:1,pet:'pet-demo-ruby',title:'정기 건강검진',date:'2026.08.18',type:'건강검진',notice:'3일 전'},{id:2,pet:'pet-demo-maru',title:'예방접종 확인',date:'2026.09.03',type:'예방접종',notice:'1주 전'}]
export default function CareSchedule(){const {pets}=getPetProfiles();const [items,setItems]=useState(base);const [open,setOpen]=useState(false);const [form,setForm]=useState({pet:pets[0]?.id||'',type:'건강검진',title:'',date:'',notice:'3일 전'});const add=()=>{if(!form.title||!form.date)return;setItems([...items,{...form,id:Date.now()}]);setOpen(false)};return <main className="schedule-page"><div className="schedule-shell"><Link to="/my" className="schedule-back">← 내 대시보드</Link><header><p>ANIMAL-ROUTE PLUS · 본선 데모</p><h1>우리 아이의 건강 일정을 챙겨요</h1><span>예방접종·검진·복약 일정을 한곳에서 확인해요.</span></header><section className="schedule-next"><b>가장 가까운 일정 · 무료</b><h2>루비 · 정기 건강검진</h2><p>8월 18일 · 16일 남음</p><button onClick={()=>setOpen(true)}>일정 관리 미리보기 →</button></section><section className="schedule-list"><div><h2>다가오는 일정 <em>Plus</em></h2><button onClick={()=>setOpen(true)}>+ 일정 추가</button></div>{items.map(i=>{const p=pets.find(x=>x.id===i.pet);return <article key={i.id}><time>{i.date}</time><span>{p?.name||'반려동물'} · {i.type}</span><strong>{i.title}</strong><small>알림 {i.notice} · 데모 상태</small></article>})}</section><footer>일정은 보호자가 직접 등록하는 관리 정보이며, 복약·진료 일정은 담당 동물병원 안내를 우선 확인해 주세요.<br/>본선 데모에서는 화면 내 상태만 표시하며 실제 저장과 푸시·문자 알림은 연결되지 않았습니다.</footer></div>{open&&<div className="schedule-backdrop" onMouseDown={e=>e.target===e.currentTarget&&setOpen(false)}><section className="schedule-modal" role="dialog" aria-modal="true"><button className="schedule-close" onClick={()=>setOpen(false)}>×</button><p>PLUS 데모 · 일정 추가</p><h2>관리할 일정을 적어주세요</h2><label>반려동물<select value={form.pet} onChange={e=>setForm({...form,pet:e.target.value})}>{pets.map(p=><option key={p.id} value={p.id}>{p.name}</option>)}</select></label><label>일정 종류<select value={form.type} onChange={e=>setForm({...form,type:e.target.value})}><option>건강검진</option><option>예방접종</option><option>복약</option><option>직접 입력</option></select></label><label>일정 이름<input value={form.title} onChange={e=>setForm({...form,title:e.target.value})} placeholder="예: 구충제 확인"/></label><label>날짜<input type="date" value={form.date} onChange={e=>setForm({...form,date:e.target.value})}/></label><div className="schedule-chips">{['당일','3일 전','1주 전','알림 없음'].map(x=><button key={x} className={form.notice===x?'active':''} onClick={()=>setForm({...form,notice:x})}>{x}</button>)}</div><button className="schedule-save" onClick={add}>화면에 일정 추가하기</button><small>실제 알림을 설정하지 않는 본선 데모입니다.</small></section></div>}</main>}
